# URL base da API consumida pelo frontend.
# Em dev: aponte para o backend local.
# Em prod (build no EasyPanel): use o caminho relativo "/api" se o Traefik proxyar,
# ou o domínio completo da API (ex.: https://api.painel.seudominio.com/api).
VITE_API_BASE_URL=http://localhost:5080/api
